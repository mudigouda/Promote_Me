import {NextResponse} from 'next/server';import {prisma} from '../../../../src/lib/prisma';import {sendWhatsAppText} from '../../../../src/lib/whatsapp';
export async function POST(request:Request){const body=await request.json();const businessId=String(body.businessId||'');const to=String(body.to||'');const question=String(body.message||'').trim();if(!businessId||!to||!question)return NextResponse.json({error:'businessId, to and message are required'},{status:400});const [contacts,campaigns,conversations]=await Promise.all([prisma.contact.count({where:{businessId}}),prisma.campaign.count({where:{businessId}}),prisma.conversation.count({where:{businessId}})]);const q=question.toLowerCase();let answer=`Your business has ${contacts} contacts, ${campaigns} campaigns and ${conversations} conversations.`;if(q.includes('contact')||q.includes('lead'))answer=`You currently have ${contacts} contacts or leads.`;else if(q.includes('campaign'))answer=`You currently have ${campaigns} campaigns.`;else if(q.includes('conversation')||q.includes('inbox'))answer=`You currently have ${conversations} conversations.`;try{const result=await sendWhatsAppText(businessId,to,answer);return NextResponse.json({ok:true,answer,result});}catch(error){return NextResponse.json({ok:false,answer,error:error instanceof Error?error.message:'WhatsApp send failed'},{status:502});}}
