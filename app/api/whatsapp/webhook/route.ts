import {NextResponse} from 'next/server';import {prisma} from '../../../../src/lib/prisma';export async function GET(request:Request){const url=new URL(request.url);const mode=url.searchParams.get('hub.mode');const token=url.searchParams.get('hub.verify_token');const challenge=url.searchParams.get('hub.challenge');if(mode==='subscribe'&&token&&challenge&&process.env.WHATSAPP_VERIFY_TOKEN===token)return new Response(challenge,{status:200});return NextResponse.json({error:'Webhook verification failed'},{status:403});}export async function POST(request:Request){try{const body=await request.json();const entries=Array.isArray(body?.entry)?body.entry:[];for(const entry of entries)for(const change of Array.isArray(entry?.changes)?entry.changes:[]){const value=change?.value;const externalId=value?.metadata?.phone_number_id;if(!externalId)continue;const account=await prisma.channelAccount.findFirst({where:{externalId,channel:'whatsapp',enabled:true}});if(!account)continue;for(const msg of Array.isArray(value?.messages)?value.messages:[]){const phone=msg?.from;if(!phone)continue;let contact=await prisma.contact.findFirst({where:{businessId:account.businessId,phone}});if(!contact)contact=await prisma.contact.create({data:{businessId:account.businessId,phone,consent:true}});const conversationId=`wa_${account.businessId}_${phone}`;await prisma.conversation.upsert({where:{id:conversationId},create:{id:conversationId,businessId:account.businessId,contactId:contact.id,channel:'whatsapp',externalId:phone,status:'open'},update:{contactId:contact.id,status:'open'}});const text=msg?.type==='text'?String(msg?.text?.body||''):msg?.type==='image'?'[Image]':msg?.type==='audio'?'[Audio]':msg?.type==='video'?'[Video]':`[${msg?.type||'message'}]`;await prisma.auditLog.create({data:{businessId:account.businessId,action:'whatsapp_message',metadata:{conversationId,direction:'inbound',phone,body:text,status:'received',providerId:msg?.id||null,type:msg?.type||'unknown'}}});}for(const s of Array.isArray(value?.statuses)?value.statuses:[]){await prisma.auditLog.create({data:{businessId:account.businessId,action:'whatsapp_status',metadata:{providerId:s?.id||null,recipient:s?.recipient_id||null,status:s?.status||'unknown',timestamp:s?.timestamp||null}}});}}return NextResponse.json({ok:true});}catch{return NextResponse.json({error:'Invalid webhook payload'},{status:400});}}