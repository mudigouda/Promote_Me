import { cookies } from "next/headers"; import { redirect } from "next/navigation"; import { COOKIE_NAME,getSessionUser } from "../../src/lib/auth"; import { prisma } from "../../src/lib/prisma";
export default async function Campaigns(){const u=await getSessionUser((await cookies()).get(COOKIE_NAME)?.value);const m=u?.memberships[0];if(!u||!m)redirect('/login');const rows=await prisma.campaign.findMany({where:{businessId:m.businessId},orderBy:{createdAt:'desc'}});return <main style={{maxWidth:1000,margin:'40px auto',padding:24,fontFamily:'system-ui'}}><p><a href="/dashboard">← Dashboard</a></p><h1>Campaigns</h1><form action="/api/campaigns" method="post" style={{display:'grid',gap:10,maxWidth:500}}><input name="name" placeholder="Campaign name" required/><select name="channel" defaultValue="email"><option>email</option><option>whatsapp</option><option>social</option><option>calling</option></select><textarea name="message" placeholder="Promotion message" required/><button type="submit">Create campaign</button></form><hr/>{rows.map(c=><article key={c.id} style={{padding:14,borderBottom:'1px solid #ddd'}}><strong>{c.name}</strong> · {c.channel} · {c.status}</article>)}</main>}
