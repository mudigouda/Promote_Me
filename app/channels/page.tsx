import {cookies} from 'next/headers';import {redirect} from 'next/navigation';import {COOKIE_NAME,getSessionUser} from '../../src/lib/auth';import {prisma} from '../../src/lib/prisma';import WhatsAppConnect from './WhatsAppConnect';export default async function Channels(){const u=await getSessionUser((await cookies()).get(COOKIE_NAME)?.value);const m=u?.memberships[0];if(!u||!m)redirect('/login');const account=await prisma.channelAccount.findFirst({where:{businessId:m.businessId,channel:'whatsapp'}});return <main className="app"><div className="shell"><a href="/dashboard">← Dashboard</a><header className="pageHead"><div><span className="pill">Channels</span><h1>Connect your channels</h1><p>Connect WhatsApp and test the connection before receiving messages.</p></div></header><section className="card"><div className="cardIcon">📱</div><h2>WhatsApp Business</h2><p>{account?`Connected: ${account.name}`:'Not connected'}</p><small>Use the Meta WhatsApp Cloud API Phone Number ID. Your access token stays in the server .env and is never sent to the browser.</small><WhatsAppConnect connected={!!account} phoneNumberId={account?.externalId||''}/></section></div></main>}