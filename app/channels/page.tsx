import { cookies } from 'next/headers';import { redirect } from 'next/navigation';import { COOKIE_NAME,getSessionUser } from '../../src/lib/auth';import { prisma } from '../../src/lib/prisma';
export default async function Channels(){const u=await getSessionUser((await cookies()).get(COOKIE_NAME)?.value);const m=u?.memberships[0];if(!u||!m)redirect('/login');const accounts=await prisma.channelAccount.findMany({where:{businessId:m.businessId},orderBy:{channel:'asc'}});return <main style={{minHeight:'100vh',background:'#f4f6f9',padding:32,fontFamily:'system-ui'}}><div style={{maxWidth:1000,margin:'auto'}}><a href="/dashboard">← Dashboard</a><h1>Channels</h1><p style={{color:'#667085'}}>Connect the communication channels your business uses.</p><section style={{background:'#fff',border:'1px solid #e4e7ec',borderRadius:16,padding:20}}><form action="/api/channels" method="post" style={{display:'grid',gridTemplateColumns:'1fr 1fr auto',gap:12}}><select name="channel"><option>WhatsApp</option><option>Facebook</option><option>Instagram</option><option>Email</option><option>Calling</option></select><input name="name" placeholder="Account name" required/><button type="submit">Add channel</button></form></section><section style={{marginTop:20,display:'grid',gap:12}}>{accounts.length?accounts.map(a=><div key={a.id} style={{background:'#fff',border:'1px solid #e4e7ec',borderRadius:14,padding:18,display:'flex',justifyContent:'space-between'}}><strong>{a.channel}</strong><span>{a.name} · {a.enabled?'Connected':'Disabled'}</span></div>):<p>No channels connected yet.</p>}</section></div></main>}
