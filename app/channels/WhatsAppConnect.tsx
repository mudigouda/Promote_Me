'use client';import {useState} from 'react';export default function WhatsAppConnect({connected,phoneNumberId}:{connected:boolean,phoneNumberId:string}){const [id,setId]=useState(phoneNumberId);const [busy,setBusy]=useState(false);const [result,setResult]=useState('');async function connect(){setBusy(true);setResult('');try{const r=await fetch('/api/whatsapp/connect',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({phoneNumberId:id})});const d=await r.json();setResult(d.ok?`Connected ${d.account.displayPhone||d.account.name}`:d.error||'Connection failed');if(d.ok)setTimeout(()=>location.reload(),700)}catch{setResult('Connection failed')}finally{setBusy(false)}}return <div className="formGrid" style={{marginTop:18}}><input value={id} onChange={e=>setId(e.target.value)} placeholder="Meta Phone Number ID"/><button className="primary" onClick={connect} disabled={busy||!id.trim()}>{busy?'Testing…':connected?'Test / reconnect':'Test & connect'}</button>{result&&<p>{result}</p>}</div>}