import {cookies} from 'next/headers';import {redirect} from 'next/navigation';import {COOKIE_NAME,getSessionUser} from '../../src/lib/auth';export default async function ImportExport(){const u=await getSessionUser((await cookies()).get(COOKIE_NAME)?.value);if(!u||!u.memberships[0])redirect('/login');return <main className="app"><div className="shell"><a href="/dashboard">← Dashboard</a><div className="pageHead"><div><h1>Import & Export</h1><p>Move business data safely in and out of Promote_Me.</p></div></div><div className="grid"><section className="card"><h2>Import contacts</h2><p>Upload a CSV. Required fields are checked before anything is saved.</p><form action="/api/import-export/contacts" method="post" encType="multipart/form-data" className="formGrid"><input type="file" name="file" accept=".csv,text/csv" required/><button className="primary">Validate & import</button></form></section><section className="card"><h2>Export contacts</h2><p>Download your business contacts as CSV.</p><a className="primary" href="/api/import-export/contacts">Export CSV</a></section><section className="card"><h2>Business backup</h2><p>Download a JSON snapshot of your business data.</p><a className="primary" href="/api/import-export/backup">Export JSON</a></section></div></div></main>}