import {cookies} from 'next/headers';import {redirect} from 'next/navigation';import {COOKIE_NAME,getSessionUser} from '../../src/lib/auth';import {prisma} from '../../src/lib/prisma';
export default async function Segments(){const u=await getSessionUser((await cookies()).get(COOKIE_NAME)?.value);const m=u?.memberships[0];if(!u||!m)redirect('/login');const rows=await prisma.audienceSegment.findMany({where:{businessId:m.businessId},orderBy:{createdAt:'desc'}});return <main className="app"><div className="shell"><a href="/dashboard">← Dashboard</a><div className="pageHead"><div><h1>Audience</h1><p>Build reusable customer segments for targeted campaigns.</p></div></div><div className="grid">{rows.map(r=><article className="card" key={r.id}><span className="pill">Segment</span><h3>{r.name}</h3><p>{JSON.stringify(r.rules)}</p></article>)}{!rows.length&&<div className="empty">Create audience segments to target customers by location, consent, tags and activity.</div>}</div></div></main>}
