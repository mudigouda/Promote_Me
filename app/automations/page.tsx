import {cookies} from 'next/headers';import {redirect} from 'next/navigation';import {COOKIE_NAME,getSessionUser} from '../../src/lib/auth';import {prisma} from '../../src/lib/prisma';
export default async function Automations(){const u=await getSessionUser((await cookies()).get(COOKIE_NAME)?.value);const m=u?.memberships[0];if(!u||!m)redirect('/login');const rows=await prisma.automation.findMany({where:{businessId:m.businessId},orderBy:{updatedAt:'desc'}});return <main className="app"><div className="shell"><a href="/dashboard">← Dashboard</a><div className="pageHead"><div><h1>Automation Studio</h1><p>Build triggers, conditions and actions that run automatically.</p></div><a className="primary" href="#create">+ New automation</a></div><div className="grid">{rows.map(r=><article className="card" key={r.id}><div className="row"><span className="pill">{r.enabled?'Active':'Paused'}</span><span>{r.trigger}</span></div><h3>{r.name}</h3><p>Action: {r.action}</p></article>)}{!rows.length&&<div className="empty">No automations yet. Create a workflow such as “new lead → assign agent → send welcome message”.</div>}</div><section id="create" className="card formCard"><h2>New automation</h2><form action="/api/automations" method="post" className="formGrid"><input name="name" placeholder="Automation name" required/><select name="trigger"><option>new_contact</option><option>message_received</option><option>campaign_replied</option><option>form_submitted</option><option>payment_received</option></select><select name="action"><option>send_message</option><option>assign_agent</option><option>add_tag</option><option>start_campaign</option><option>notify_team</option></select><button className="primary">Create automation</button></form></section></div></main>}
