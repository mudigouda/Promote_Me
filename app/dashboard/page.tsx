import { cookies } from "next/headers";
import { redirect } from "next/navigation";
import { COOKIE_NAME, getSessionUser } from "../../src/lib/auth";
export default async function Dashboard(){const u=await getSessionUser((await cookies()).get(COOKIE_NAME)?.value);const m=u?.memberships[0];if(!u||!m)redirect('/login');const links=[['/settings','Business Settings'],['/contacts','Contacts'],['/campaigns','Campaigns']];return <main style={{minHeight:'100vh',padding:32,fontFamily:'system-ui'}}><header style={{display:'flex',justifyContent:'space-between',alignItems:'center',flexWrap:'wrap',gap:16}}><div><h1>Promote_Me</h1><p>Marketing workspace</p></div><form action="/api/auth/logout" method="post"><button type="submit">Log out</button></form></header><h2>Welcome{u.name?`, ${u.name}`:''}!</h2><p>Business: <strong>{m.business.name}</strong> · Role: <strong>{m.role}</strong></p><nav style={{display:'flex',gap:12,flexWrap:'wrap',margin:'28px 0'}}>{links.map(([href,label])=><a key={href} href={href} style={{padding:'14px 18px',border:'1px solid #ddd',borderRadius:10}}>{label}</a>)}</nav><section><h2>Workspace</h2><p>Manage contacts, campaigns and business settings from the links above.</p></section></main>}
