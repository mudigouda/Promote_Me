import { cookies } from "next/headers";import { redirect } from "next/navigation";import { COOKIE_NAME,getSessionUser } from "../../src/lib/auth";
export default async function Dashboard(){const u=await getSessionUser((await cookies()).get(COOKIE_NAME)?.value);const m=u?.memberships[0];if(!u||!m)redirect('/login');const links=[['/settings','⚙️ Business Settings'],['/team','👥 Team'],['/contacts','👤 Contacts'],['/campaigns','📣 Campaigns']];return <main style={{minHeight:'100vh',background:'#f6f7fb',fontFamily:'system-ui'}}><div style={{display:'flex',minHeight:'100vh'}}><aside style={{width:230,background:'#111827',color:'#fff',padding:24,boxSizing:'border-box'}}><h2>Promote_Me</h2><p style={{color:'#9ca3af',fontSize:13}}>{m.business.name}</p><nav style={{display:'grid',gap:8,marginTop:28}}>{links.map(([href,label])=><a key={href} href={href} style={{color:'#fff',textDecoration:'none',padding:'11px 10px',borderRadius:8}}>{label}</a>)}</nav></aside><section style={{flex:1,padding:'28px 32px'}}><header style={{display:'flex',justifyContent:'space-between',alignItems:'center',gap:16}}><div><h1 style={{margin:0}}>Dashboard</h1><p style={{color:'#667085'}}>Welcome{u.name?`, ${u.name}`:''}</p></div><form action="/api/auth/logout" method="post"><button type="submit">Log out</button></form></header><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(210px,1fr))',gap:16,marginTop:28}}>{[['Contacts','Manage your customer and lead database.','/contacts'],['Campaigns','Create and organize promotions.','/campaigns'],['Team','Manage access and assignments.','/team'],['Settings','Update business information.','/settings']].map(([title,text,href])=><a key={title} href={href} style={{background:'#fff',border:'1px solid #e4e7ec',borderRadius:14,padding:20,textDecoration:'none',color:'#101828'}}><h3>{title}</h3><p style={{color:'#667085'}}>{text}</p></a>)}</div></section></div></main>}
