import {cookies} from 'next/headers';import {redirect} from 'next/navigation';import {COOKIE_NAME,getSessionUser} from '../../src/lib/auth';export default async function Dashboard(){const u=await getSessionUser((await cookies()).get(COOKIE_NAME)?.value);const m=u?.memberships[0];if(!u||!m)redirect('/login');const links=[['/contacts','Contacts'],['/campaigns','Campaigns'],['/templates','Templates'],['/segments','Audience'],['/inbox','Inbox'],['/automations','Automation'],['/analytics','Analytics'],['/team','Team'],['/channels','Channels'],['/settings','Settings']];return <main className="app"><div className="shell"><div className="pageHead"><div><span className="pill">{m.role}</span><h1>Good to see you{u.name?`, ${u.name}`:''}.</h1><p>{m.business.name} · Your marketing command center</p></div><form action="/api/auth/logout" method="post"><button>Log out</button></form></div><div className="grid">{links.map(([href,label])=><a className="card" key={href} href={href} style={{textDecoration:'none'}}><span className="pill">Workspace</span><h3>{label}</h3><p>Open {label.toLowerCase()} and manage your business.</p></a>)}</div></div></main>}