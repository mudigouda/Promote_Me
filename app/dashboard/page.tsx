import {cookies} from 'next/headers';import {redirect} from 'next/navigation';import {COOKIE_NAME,getSessionUser} from '../../src/lib/auth';export default async function Dashboard(){const u=await getSessionUser((await cookies()).get(COOKIE_NAME)?.value);const m=u?.memberships[0];if(!u||!m)redirect('/login');const links=[['/contacts','Contacts','Customers & leads'],['/campaigns','Campaigns','Create and measure campaigns'],['/templates','Templates','Reusable message library'],['/segments','Audience','Target the right customers'],['/inbox','Inbox','Customer conversations'],['/automations','Automation','Triggers and workflows'],['/chatbots','Chatbots','Automated journeys'],['/forms','Lead Forms','Capture new leads'],['/analytics','Analytics','Reports and performance'],['/team','Team','Users and permissions'],['/channels','Channels','Connected accounts'],['/settings','Settings','Business configuration']];return <main className="app"><div className="shell"><div className="pageHead"><div><span className="pill">{m.role}</span><h1>Good to see you{u.name?`, ${u.name}`:''}.</h1><p>{m.business.name} · Marketing command center</p></div><form action="/api/auth/logout" method="post"><button>Log out</button></form></div><div className="grid">{links.map(([href,label,desc])=><a className="card" key={href} href={href} style={{textDecoration:'none'}}><h3>{label}</h3><p>{desc}</p></a>)}</div></div></main>}