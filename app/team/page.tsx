import { cookies } from "next/headers";
import { redirect } from "next/navigation";
import { COOKIE_NAME,getSessionUser } from "../../src/lib/auth";
import { prisma } from "../../src/lib/prisma";
export default async function Team(){const u=await getSessionUser((await cookies()).get(COOKIE_NAME)?.value);const m=u?.memberships[0];if(!u||!m)redirect('/login');if(!['OWNER','ADMIN'].includes(m.role))return <main style={{padding:32,fontFamily:'system-ui'}}><a href="/dashboard">← Dashboard</a><h1>Team</h1><p>You do not have permission to manage the team.</p></main>;const members=await prisma.membership.findMany({where:{businessId:m.businessId},include:{user:true},orderBy:{createdAt:'asc'}});return <main style={{minHeight:'100vh',background:'#f6f7fb',padding:'32px 20px',fontFamily:'system-ui'}}><div style={{maxWidth:1000,margin:'auto'}}><a href="/dashboard">← Dashboard</a><h1>Team</h1><p style={{color:'#667085'}}>Manage people who can access this business.</p><section style={{background:'#fff',border:'1px solid #e4e7ec',borderRadius:14,padding:20}}><form action="/api/team" method="post" style={{display:'grid',gridTemplateColumns:'2fr 1fr auto',gap:10}}><input name="email" type="email" placeholder="User email" required/><select name="role" defaultValue="VIEWER"><option>ADMIN</option><option>MARKETING</option><option>SALES</option><option>SUPPORT</option><option>VIEWER</option></select><button type="submit">Add to team</button></form></section><section style={{marginTop:20,background:'#fff',border:'1px solid #e4e7ec',borderRadius:14,overflow:'hidden'}}>{members.map(x=><div key={x.id} style={{padding:16,borderBottom:'1px solid #eef0f3',display:'flex',justifyContent:'space-between',gap:12}}><div><strong>{x.user.name||x.user.email}</strong><div style={{color:'#667085'}}>{x.user.email}</div></div><span style={{fontWeight:600}}>{x.role}</span></div>)}</section></div></main>}
