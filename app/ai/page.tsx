import {cookies} from 'next/headers';import {redirect} from 'next/navigation';import {COOKIE_NAME,getSessionUser} from '../../src/lib/auth';import Chat from './Chat';export default async function AI(){const u=await getSessionUser((await cookies()).get(COOKIE_NAME)?.value);if(!u||!u.memberships[0])redirect('/login');return <main className="app"><div className="shell"><a href="/dashboard">← Dashboard</a><div className="pageHead"><div><span className="pill">AI Assistant</span><h1>Business AI</h1><p>Chat naturally with your business data, reports and conversations.</p></div></div><section className="card" style={{maxWidth:900,margin:'auto'}}><Chat/></section></div></main>}