import {cookies} from 'next/headers';import {redirect} from 'next/navigation';import {COOKIE_NAME,getSessionUser} from '../../src/lib/auth';export default async function AI(){const u=await getSessionUser((await cookies()).get(COOKIE_NAME)?.value);if(!u||!u.memberships[0])redirect('/login');return <main className="app"><div className="shell"><a href="/dashboard">← Dashboard</a><div className="pageHead"><div><span className="pill">AI</span><h1>Business AI</h1><p>Ask questions about your contacts, campaigns, conversations and performance.</p></div></div><section className="card" style={{maxWidth:850,margin:'auto'}}><div className="empty" style={{marginBottom:20}}><h2>Ask your business anything</h2><p>Try “How many contacts do I have?”, “What are my best campaigns?” or “Summarize my WhatsApp performance this month.”</p></div><form action="/api/ai" method="post" className="formGrid"><textarea name="message" required placeholder="Ask about your business data…"/><button className="primary">Ask AI</button></form></section></div></main>}
