import {cookies} from 'next/headers';import {redirect} from 'next/navigation';import {COOKIE_NAME,getSessionUser} from '../../src/lib/auth';import {prisma} from '../../src/lib/prisma';
export default async function Templates(){const u=await getSessionUser((await cookies()).get(COOKIE_NAME)?.value);const m=u?.memberships[0];if(!u||!m)redirect('/login');const rows=await prisma.messageTemplate.findMany({where:{businessId:m.businessId},orderBy:{updatedAt:'desc'}});return <main className="app"><div className="shell"><a href="/dashboard">← Dashboard</a><div className="pageHead"><div><h1>Message Templates</h1><p>Create reusable WhatsApp-ready messages with variables and buttons.</p></div><a className="primary" href="#create">+ New template</a></div><div className="grid">{rows.map(r=><article className="card" key={r.id}><div className="row"><span className="pill">{r.status}</span><span>{r.language}</span></div><h3>{r.name}</h3><p>{r.body}</p><small>{r.category}</small></article>)}{!rows.length&&<div className="empty">No templates yet. Create your first reusable message.</div>}</div><section id="create" className="card formCard"><h2>Create template</h2><form action="/api/templates" method="post" className="formGrid"><input name="name" placeholder="Template name" required/><select name="category"><option>Marketing</option><option>Utility</option><option>Authentication</option></select><input name="language" defaultValue="en" placeholder="Language"/><textarea name="body" placeholder="Hello {{1}}, we have an offer for you..." required/><button className="primary">Save template</button></form></section></div></main>}
