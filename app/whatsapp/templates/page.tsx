import {cookies} from 'next/headers';import {redirect} from 'next/navigation';import {COOKIE_NAME,getSessionUser} from '../../../src/lib/auth';import {prisma} from '../../../src/lib/prisma';export default async function WhatsAppTemplates(){const u=await getSessionUser((await cookies()).get(COOKIE_NAME)?.value);const m=u?.memberships[0];if(!u||!m)redirect('/login');const templates=await prisma.messageTemplate.findMany({where:{businessId:m.businessId},orderBy:{createdAt:'desc'},take:50});return <main className="app"><div className="shell"><a href="/whatsapp">← WhatsApp</a><header className="pageHead"><div><span className="pill">Templates</span><h1>WhatsApp Message Templates</h1><p>Manage reusable messages for campaigns and customer conversations.</p></div><a className="primary" href="/templates">Manage templates</a></header><div className="grid">{templates.length?templates.map(t=><article className="card" key={t.id}><div className="cardIcon">📝</div><h2>{t.name}</h2><p>{t.body}</p><small>{t.category} · {t.language} · {t.status} · {t.createdAt.toLocaleDateString()}</small></article>):<div className="empty">No templates yet. Create one to start building WhatsApp campaigns.</div>}</div></div></main>}