'use client';import {useState} from 'react';export default function Composer(){const [name,setName]=useState('');const [audience,setAudience]=useState('all');const [template,setTemplate]=useState('');const [schedule,setSchedule]=useState('now');const [busy,setBusy]=useState(false);const [result,setResult]=useState('');async function submit(){if(!name.trim()||!template.trim())return;setBusy(true);setResult('');try{const r=await fetch('/api/whatsapp/broadcasts',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,audience,template,schedule})});const d=await r.json();setResult(d.message||d.error||'Saved');}catch{setResult('Could not save broadcast.')}finally{setBusy(false)}}return <section className="card formCard"><h2>Create broadcast</h2><div className="formGrid"><input value={name} onChange={e=>setName(e.target.value)} placeholder="Campaign name"/><select value={audience} onChange={e=>setAudience(e.target.value)}><option value="all">All eligible contacts</option><option value="consented">Consented contacts</option></select><textarea value={template} onChange={e=>setTemplate(e.target.value)} placeholder="Message / template content…"/><select value={schedule} onChange={e=>setSchedule(e.target.value)}><option value="now">Send now</option><option value="schedule">Schedule for later</option></select><button className="primary" onClick={submit} disabled={busy}>{busy?'Saving…':'Create broadcast'}</button>{result&&<p>{result}</p>}</div></section>}