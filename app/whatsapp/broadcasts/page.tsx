import {cookies} from 'next/headers';import {redirect} from 'next/navigation';import {COOKIE_NAME,getSessionUser} from '../../../src/lib/auth';import {prisma} from '../../../src/lib/prisma';export default async function Broadcasts(){const u=await getSessionUser((await cookies()).get(COOKIE_NAME)?.value);const m=u?.memberships[0];if(!u||!m)redirect('/login');const campaigns=await prisma.campaign.findMany({where:{businessId:m.businessId},orderBy:{createdAt:'desc'},take:20});return <main className="app"><div className="shell"><a href="/whatsapp">← WhatsApp</a><header className="pageHead"><div><span className="pill">Broadcasts</span><h1>WhatsApp Broadcasts</h1><p>Create, review and track customer broadcasts.</p></div><a className="primary" href="/campaigns">Create campaign</a></header><div className="grid">{campaigns.length?campaigns.map(c=><article className="card" key={c.id}><div className="cardIcon">📣</div><h2>{c.name}</h2><p>{c.status} · {c.createdAt.toLocaleDateString()}</p><a href={`/campaigns/${c.id}`}>Open campaign →</a></article>):<div className="empty">No broadcasts yet. Create your first WhatsApp campaign.</div>}</div></div></main>}
