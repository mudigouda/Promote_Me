import {cookies} from 'next/headers';import {redirect} from 'next/navigation';import {COOKIE_NAME,getSessionUser} from '../../../src/lib/auth';import {prisma} from '../../../src/lib/prisma';import Composer from './Composer';export default async function Broadcasts(){const u=await getSessionUser((await cookies()).get(COOKIE_NAME)?.value);const m=u?.memberships[0];if(!u||!m)redirect('/login');const campaigns=await prisma.campaign.findMany({where:{businessId:m.businessId,channel:'whatsapp'},orderBy:{createdAt:'desc'},take:20});return <main className="app"><div className="shell"><a href="/whatsapp">← WhatsApp</a><header className="pageHead"><div><span className="pill">Broadcasts</span><h1>WhatsApp Broadcasts</h1><p>Create audience-based campaign drafts and scheduled campaigns.</p></div></header><Composer/><div className="grid" style={{marginTop:20}}>{campaigns.length?campaigns.map(c=><article className="card" key={c.id}><div className="cardIcon">📣</div><h2>{c.name}</h2><p>{c.status} · {c.createdAt.toLocaleDateString()}</p><p>{c.message}</p></article>):<div className="empty">No broadcasts yet. Create your first one above.</div>}</div></div></main>}