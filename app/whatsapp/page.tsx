import {cookies} from 'next/headers';import {redirect} from 'next/navigation';import {COOKIE_NAME,getSessionUser} from '../../src/lib/auth';import {prisma} from '../../src/lib/prisma';
export default async function WhatsApp(){const u=await getSessionUser((await cookies()).get(COOKIE_NAME)?.value);const m=u?.memberships[0];if(!u||!m)redirect('/login');const account=await prisma.channelAccount.findFirst({where:{businessId:m.businessId,channel:'whatsapp'}});const conversations=await prisma.conversation.findMany({where:{businessId:m.businessId,channel:'whatsapp'},orderBy:{updatedAt:'desc'},take:20});return <main className="app"><div className="shell"><header className="pageHead"><div><a href="/dashboard">← Dashboard</a><h1>WhatsApp</h1><p>Manage customer messages and campaigns.</p></div><a className="primary" href="/channels">{account?'✓ Connected':'Connect WhatsApp'}</a></header><div className="grid" style={{gridTemplateColumns:'2fr 1fr'}}><section className="card"><div className="row"><div><h2>Inbox</h2><p>Recent customer conversations</p></div><a href="/whatsapp/broadcasts">New broadcast</a></div>{conversations.length===0?<div className="empty">No conversations yet.</div>:<div className="inbox">{conversations.map(c=><a className="conversation" href={`/whatsapp/conversation?id=${c.id}`} key={c.id} style={{textDecoration:'none',color:'inherit'}}><div className="avatar">WA</div><div className="grow"><strong>{c.externalId||'WhatsApp contact'}</strong><p>{c.status} · {c.updatedAt.toLocaleString()}</p></div></a>)}</div>}</section><aside className="card"><h2>Tools</h2><a className="nav" href="/whatsapp/broadcasts">📣 <span>Broadcasts</span></a><a className="nav" href="/whatsapp/templates">📝 <span>Templates</span></a><a className="nav" href="/automations">⚡ <span>Automations</span></a><a className="nav" href="/ai">✨ <span>Ask AI</span></a></aside></div></div></main>}