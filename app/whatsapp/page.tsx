import {cookies} from 'next/headers';import {redirect} from 'next/navigation';import {COOKIE_NAME,getSessionUser} from '../../src/lib/auth';import {prisma} from '../../src/lib/prisma';
export default async function WhatsApp(){const u=await getSessionUser((await cookies()).get(COOKIE_NAME)?.value);const m=u?.memberships[0];if(!u||!m)redirect('/login');const account=await prisma.channelAccount.findFirst({where:{businessId:m.businessId,channel:'whatsapp'}});const conversations=await prisma.conversation.findMany({where:{businessId:m.businessId,channel:'whatsapp'},orderBy:{updatedAt:'desc'},take:20});return <main className="app"><div className="shell"><a href="/dashboard">← Dashboard</a><header className="pageHead"><div><span className="pill">WhatsApp</span><h1>WhatsApp</h1><p>Messages, campaigns and templates in one place.</p></div><a className="primary" href="/channels">📱 {account?'Connected':'Connect WhatsApp'}</a></header><section className="card"><div className="row"><div><h2>Inbox</h2><p>{conversations.length} recent conversation{conversations.length===1?'':'s'}</p></div><a href="/whatsapp/broadcasts">New broadcast →</a></div>{conversations.length===0?<div className="empty">No messages yet. Connect WhatsApp and send a test message.</div>:<div className="inbox">{conversations.map(c=><a className="conversation" href={`/whatsapp/conversation?id=${c.id}`} key={c.id} style={{textDecoration:'none',color:'inherit'}}><div className="avatar">WA</div><div className="grow"><strong>{c.externalId||'WhatsApp contact'}</strong><p>{c.status} · {c.updatedAt.toLocaleString()}</p></div></a>)}</div>}</section><div className="grid" style={{marginTop:20}}><a className="card" href="/whatsapp/broadcasts" style={{textDecoration:'none',color:'inherit'}}><div className="cardIcon">📣</div><h2>Broadcasts</h2><p>Send campaigns to your audience.</p></a><a className="card" href="/whatsapp/templates" style={{textDecoration:'none',color:'inherit'}}><div className="cardIcon">📝</div><h2>Templates</h2><p>Manage reusable messages.</p></a><a className="card" href="/automations" style={{textDecoration:'none',color:'inherit'}}><div className="cardIcon">⚡</div><h2>Automations</h2><p>Automate customer replies.</p></a></div></div></main>}