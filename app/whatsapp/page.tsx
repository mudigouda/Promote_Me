import {cookies} from 'next/headers';import {redirect} from 'next/navigation';import {COOKIE_NAME,getSessionUser} from '../../src/lib/auth';import {prisma} from '../../src/lib/prisma';export default async function WhatsApp(){const u=await getSessionUser((await cookies()).get(COOKIE_NAME)?.value);const m=u?.memberships[0];if(!u||!m)redirect('/login');const account=await prisma.channelAccount.findFirst({where:{businessId:m.businessId,channel:'whatsapp'}});const conversations=await prisma.conversation.findMany({where:{businessId:m.businessId,channel:'whatsapp'},orderBy:{updatedAt:'desc'},take:30});return <main className="app"><div className="shell"><a href="/dashboard">← Dashboard</a><header className="pageHead"><div><span className="pill">WhatsApp</span><h1>WhatsApp Center</h1><p>Inbox, automation, broadcasts and AI conversations.</p></div><a className="primary" href="/ai">✨ Ask AI</a></header><div className="grid"><section className="card"><div className="cardIcon">📱</div><h2>Connection</h2><p>{account?'WhatsApp account connected':'No WhatsApp account connected yet.'}</p><small>{account?.name||'Connect a WhatsApp Business account in Channels.'}</small></section><section className="card"><div className="cardIcon">💬</div><h2>{conversations.length}</h2><p>Recent WhatsApp conversations</p></section><section className="card"><div className="cardIcon">🤖</div><h2>AI assistant</h2><p>Use AI to answer business questions and assist conversations.</p><a className="primary" href="/ai">Open AI chat</a></section></div><section className="card" style={{marginTop:20}}><div className="row"><div><h2>Inbox</h2><p>Recent WhatsApp conversations</p></div><a href="/inbox">Open full inbox →</a></div>{conversations.length===0?<div className="empty">No WhatsApp conversations yet.</div>:<div className="inbox">{conversations.map(c=><a className="conversation" href={`/inbox?conversation=${c.id}`} key={c.id} style={{textDecoration:'none',color:'inherit'}}><div className="avatar">WA</div><div className="grow"><strong>{c.externalId||'WhatsApp contact'}</strong><p>{c.status} · {c.updatedAt.toLocaleString()}</p></div></a>)}</div>}</section><div className="grid" style={{marginTop:20}}><a className="card" href="/campaigns"><div className="cardIcon">📣</div><h2>Broadcasts</h2><p>Create and manage WhatsApp campaigns.</p></a><a className="card" href="/templates"><div className="cardIcon">📝</div><h2>Templates</h2><p>Manage reusable WhatsApp message templates.</p></a><a className="card" href="/automations"><div className="cardIcon">⚡</div><h2>Automations</h2><p>Build automated customer journeys.</p></a><a className="card" href="/reports"><div className="cardIcon">📊</div><h2>Analytics</h2><p>Track delivery, replies and conversions.</p></a></div></div></main>}
