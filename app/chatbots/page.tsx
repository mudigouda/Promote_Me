import {cookies} from 'next/headers';import {redirect} from 'next/navigation';import {COOKIE_NAME,getSessionUser} from '../../src/lib/auth';import {prisma} from '../../src/lib/prisma';
export default async function Chatbots(){const u=await getSessionUser((await cookies()).get(COOKIE_NAME)?.value);const m=u?.memberships[0];if(!u||!m)redirect('/login');const rows=await prisma.chatbotFlow.findMany({where:{businessId:m.businessId},include:{nodes:true},orderBy:{updatedAt:'desc'}});return <main className="app"><div className="shell"><a href="/dashboard">← Dashboard</a><div className="pageHead"><div><h1>AI Chatbot Studio</h1><p>Design automated customer journeys and hand off to your team.</p></div><a className="primary" href="#create">+ New flow</a></div><div className="grid">{rows.map(r=><article className="card" key={r.id}><span className="pill">{r.status}</span><h3>{r.name}</h3><p>{r.nodes.length} flow nodes · Trigger: {r.trigger||'Not configured'}</p></article>)}{!rows.length&&<div className="empty">No chatbot flows yet. Create a welcome, lead qualification or support flow.</div>}</div><section id="create" className="card formCard"><h2>Create chatbot flow</h2><form action="/api/chatbots" method="post" className="formGrid"><input name="name" placeholder="Flow name" required/><select name="trigger"><option>keyword</option><option>new_conversation</option><option>form_submitted</option><option>campaign_reply</option></select><button className="primary">Create flow</button></form></section></div></main>}
