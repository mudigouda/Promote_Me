import {cookies} from 'next/headers';import {redirect} from 'next/navigation';import {COOKIE_NAME,getSessionUser} from '../../src/lib/auth';import {prisma} from '../../src/lib/prisma';export default async function Analytics(){const u=await getSessionUser((await cookies()).get(COOKIE_NAME)?.value);const m=u?.memberships[0];if(!u||!m)redirect('/login');const [contacts,campaigns,conversations,templates]=await Promise.all([prisma.contact.count({where:{businessId:m.businessId}}),prisma.campaign.count({where:{businessId:m.businessId}}),prisma.conversation.count({where:{businessId:m.businessId}}),prisma.messageTemplate.count({where:{businessId:m.businessId}})]);return <main className="app"><div className="shell"><a href="/dashboard">← Dashboard</a><div className="pageHead"><div><h1>Analytics</h1><p>A clean overview of your marketing workspace.</p></div></div><div className="grid">{[['Contacts',contacts,'Total leads and customers'],['Campaigns',campaigns,'Campaigns created'],['Conversations',conversations,'Inbox conversations'],['Templates',templates,'Reusable messages']].map(([name,n,sub])=><article className="card" key={String(name)}><small>{name}</small><h2 style={{fontSize:38,margin:'10px 0'}}>{n}</h2><p>{sub}</p></article>)}</div><section className="card formCard"><h2>Performance center</h2><p>Delivery, read, click, reply and conversion charts will populate here as real provider events are connected.</p></section></div></main>}
