import {cookies} from 'next/headers';import {redirect} from 'next/navigation';import {COOKIE_NAME,getSessionUser} from '../../src/lib/auth';import {prisma} from '../../src/lib/prisma';export default async function Forms(){const u=await getSessionUser((await cookies()).get(COOKIE_NAME)?.value);const m=u?.memberships[0];if(!u||!m)redirect('/login');const rows=await prisma.whatsAppForm.findMany({where:{businessId:m.businessId},orderBy:{updatedAt:'desc'}});return <main className="app"><div className="shell"><a href="/dashboard">← Dashboard</a><div className="pageHead"><div><h1>Lead Forms</h1><p>Capture leads with simple WhatsApp-ready forms.</p></div><a className="primary" href="#create">+ New form</a></div><div className="grid">{rows.map(r=><article className="card" key={r.id}><span className="pill">{r.status}</span><h3>{r.name}</h3><p>{r.description||'No description'}</p></article>)}{!rows.length&&<div className="empty">No forms yet. Create a lead capture form.</div>}</div><section id="create" className="card formCard"><h2>Create form</h2><form action="/api/forms" method="post" className="formGrid"><input name="name" placeholder="Form name" required/><input name="description" placeholder="Short description"/><button className="primary">Create form</button></form></section></div></main>}
